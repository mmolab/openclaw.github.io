---
import BaseLayout from './BaseLayout.astro';
import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';

const { title, description, lang = 'en' } = Astro.props;

const nav = [
  { label: lang === 'en' ? 'Articles' : 'Bài viết', href: `/${lang}/blog` },
  { label: lang === 'en' ? 'About' : 'Giới thiệu', href: `/about` },
  { label: lang === 'en' ? 'Contact' : 'Liên hệ', href: `/contact` },
];
---

<BaseLayout title={title} description={description}>
  <Header
    slot="header"
    position="fixed"
    size="md"
    nav={nav}
    showCta={false}
    actions={[
      {
        icon: 'globe',
        href: lang === 'en' ? '/vi/' : '/en/',
        label: lang === 'en' ? 'Tiếng Việt 🇻🇳' : 'English 🇺🇸',
        iconOnly: false,
      },
    ]}
  />

  <main>
    <slot />
  </main>

  <Footer slot="footer" layout="simple" background="secondary" showCopyright={true} />
</BaseLayout>
